import tailwindcss from '@tailwindcss/postcss'; // Correct import for the PostCSS plugin
import autoprefixer from 'autoprefixer';

export default {
  plugins: [
    tailwindcss,
    autoprefixer,
  ],
};